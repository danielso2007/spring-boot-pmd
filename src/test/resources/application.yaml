server:
  port: 8080
  servlet:
    context-path: "/rest"
spring:
  application:
    name: "@project.artifactId@-@project.version@"
logging:
  level:
    org:
      springframework: INFO
    com:
      example:
        spring: INFO
